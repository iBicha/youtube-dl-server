{
  "name": "youtube-server",
  "version": "1.0.0",
  "description": "Youtube Rest API, powered by youtube-dl",
  "main": "dist/app.js",
  "scripts": {
    "start": "tsc && node .",
    "prestart": "mkdir -p tools/bin && wget https://yt-dl.org/downloads/latest/youtube-dl -O tools/bin/youtube-dl && chmod a+rx tools/bin/youtube-dl",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.9",
    "tslint": "^6.1.3",
    "typescript": "^4.1.2"
  }
}
